$screen ?= 'only screen';

// Base Mixin
bp($min, $mq = max-width, $type = $screen)
  if $type != '' and $type != null
    $type = $type + ' and';

  $query = $type + ' (' + $mq + ': ' + $min + ')';

  if typeof($min) is string
    $query = $min;
  @media $query
    {block}
